describe("ChamberController", function() {
  var controller;

  beforeEach(function() {
    controller = new ChamberController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
