describe("ParticlesTestController", function() {
  var controller;

  beforeEach(function() {
    controller = new ParticlesTestController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
