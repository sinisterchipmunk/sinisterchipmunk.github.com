describe("DoorController", function() {
  var controller;

  beforeEach(function() {
    controller = new DoorController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
