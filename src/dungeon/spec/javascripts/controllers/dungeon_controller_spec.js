describe("DungeonController", function() {
  var controller;

  beforeEach(function() {
    controller = new DungeonController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
