describe("HatchController", function() {
  var controller;

  beforeEach(function() {
    controller = new HatchController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
