describe("TamhatchController", function() {
  var controller;

  beforeEach(function() {
    controller = new TamhatchController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
