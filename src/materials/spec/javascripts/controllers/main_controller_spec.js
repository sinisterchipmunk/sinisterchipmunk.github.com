describe("MainController", function() {
  var controller;

  beforeEach(function() {
    controller = new MainController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
