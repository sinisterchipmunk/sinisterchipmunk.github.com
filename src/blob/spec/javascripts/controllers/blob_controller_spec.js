describe("BlobController", function() {
  var controller;

  beforeEach(function() {
    controller = new BlobController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
