describe("MeadowController", function() {
  var controller;

  beforeEach(function() {
    controller = new MeadowController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
